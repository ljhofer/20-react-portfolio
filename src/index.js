// Imports packages and files needed
import React from 'react';
import ReactDOM from 'react-dom';
import './index.css';
import App from './App';

// Renders the App component to the HTML
ReactDOM.render(
  <App />,
  document.getElementById('root')
);


